import { useQuery } from '@tanstack/react-query';
import { useActor } from './useActor';

// This file is reserved for React Query hooks that interact with the backend
// Currently no queries are needed for the public-facing website
// Future admin features would add queries here (e.g., useGetAllEnquiries)
